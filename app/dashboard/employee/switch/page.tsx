import { EmployeeWorkRouteShell } from "@/components/client/employee-work-route-shell";

export default function EmployeeSiteSwitchPage() {
  return (
    <EmployeeWorkRouteShell
      title="Switch site"
      description="Move to another work site mid-day with GPS and selfie. Requires an open check-in session."
      section="switch"
    />
  );
}
